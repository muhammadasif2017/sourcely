"""Every table, imported in one place so Alembic's autogenerate sees the full schema.

Tables are added by the Phase 1 tasks: accounts (Task 13), workspaces (Task 14), API keys
(Task 15), documents and chunks (Task 16).
"""
